import XCTest
@testable import CoreDataSearchComposer

final class CoreDataSearchComposerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CoreDataSearchComposer().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
